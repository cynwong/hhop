{{> carousel_block }}

<div class="album py-5 bg-light">
  <div class="container">
    <div class="row">
      {{#each recipes }}
        {{> recipe_card recipe=this.recipe}}
      {{/each}}
    </div>
  </div>
</div>