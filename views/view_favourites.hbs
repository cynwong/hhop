{{> carousel_block }}

<div class="album py-5 bg-light">
  <div class="container">
    <div class="row">
      {{#each recipes }}
        {{> recipe_card this.dataValues}}
      {{/each}}
    </div>
  </div>
</div>